# PROJECT-CONTEXT.md — parkstatus.today

Canonical facts and house rules for every session. The prompt-engineer and builder
workflows (`.claude/commands/prompt-engineer.md`, `.claude/commands/builder.md`) read this
file first. Treat everything here as known — do not re-ask the user for it; flag it if the
user says something that contradicts it.

**Last verified against the repo: 2026-09-07.** Line numbers are approximate — confirm by
reading the file before relying on one.

---

## What the project is

A live "is this park open?" status board at https://parkstatus.today, plus an iOS app
(Capacitor wrapper). Coverage: 474 NPS units + NY/CA/TX/MN/FL/WA state parks + 112
National Forests (wildfire proximity) + 358 NY beaches. Every status is the site's
*reading* of official public data, refreshed hourly — not an official determination.
Sister site: half-mast.com (shared visual language).

"Done" for a typical task = the change is live on parkstatus.today (or in a TestFlight
build for iOS), verified on the real surface, with the coordination files updated.

## Repo layout & key files

- `public_html/` — the entire static site (this is what FTP-deploys to Hostinger).
  - `index.html` (~1170 lines) — the map/status-board homepage. Inline `<style>`,
    vanilla JS, no framework, no build step. Leaflet 1.9.4 + markercluster 1.5.3 from
    cdnjs. Sections: header/nav, status ticker, hero, `.board` (map + rail), the unified
    "Alerts" panel, "Built for confidence", "Official source health", footer, `#detail`
    slide-in park card.
  - `park/<slug>/index.html` — ~1290 generated per-park pages. Do NOT hand-edit; regen
    from `build-parks.js`.
  - `park/park.css` — generated stylesheet for park pages + `/park/` directory, from the
    `PARK_CSS` template string in build-parks.js (~693).
  - `road/<slug>/index.html` + `road/index.html` — generated road-status pages (added
    2026-09-07). From `roads.json`.
  - `beach/ny-<county>-county/index.html` — NY beach county hubs (generated).
  - `guides/*.html` — flat guide articles + `guides/index.html` + `guides.css`.
  - `privacy.html`, `support.html` — the App Store privacy/support URLs.
  - `sw.js` — web-push service worker. `app-native.js` — Capacitor-only shim (adds
    `.in-app` to `<html>`, native APNs, syncs `ps_follows` + `ps_alert_prefs` to the
    Worker; no-ops on web).
  - `shutdown.json` — consumed by the `#shutdown-banner` div on every page.
  - `parks-enriched.json`, `parks.json`, `sitemap.xml`, `llms.txt`, `forests.json` — all
    generated by build-parks.js.
- `build-parks.js` — Node, **ZERO npm dependencies** (stdlib only).
  `NPS_API_KEY=xxx node build-parks.js` (~10 min, Wikipedia lookups). Regenerates park
  pages, `/park/` index, road pages, beach hubs, `parks-enriched.json`, `parks.json`,
  `sitemap.xml`, `llms.txt`, `forests.json`. Run daily by
  `.github/workflows/refresh-park-data.yml`. Key functions (approx lines): `hoursSpec()`
  112 · `npsRich()` 180 (NPS API: description, addresses, contacts, operatingHours,
  images, directionsInfo, weatherInfo, entranceFees, url, fullName) · `npsAddress()` 196
  · `npsHours()` 202 · `npsFee()` 213 · `npsAlerts()` (Tier B road status; NPS alerts
  API, paginated like npsRich) · `collectEntities()` 237 · `buildForests()` 343
  (keyless server-side ArcGIS fetch pattern — `FS_BOUNDARIES`) · `siteNav()` (the one
  nav helper — replaced the former 4 duplicated strings) · `visitorBlock(en)` 384 ·
  `pageHtml(e, en, updatedISO, tally)` 407 (park page; JSON-LD @graph = BreadcrumbList +
  FAQPage + TouristAttraction/Park + openingHoursSpecification; also renders the "Roads
  in this park" block from a reverse index built once in `main()`) · `roadPageHtml()` /
  `roadIndexHtml()` · `roadStatus()` · `directoryHtml()` 625 · `PARK_CSS` 693 ·
  `sitemap()` 801 · `llmsTxt()` 826 · `beachHubHtml()` 880 · `main()` 1106.
- `reservations.json` (repo root) — curated parks requiring a timed-entry / vehicle
  reservation, keyed by entity id, verified against nps.gov + recreation.gov for 2026.
  `_note` says re-verify each spring. Arches/Glacier/Mount Rainier/Yosemite deliberately
  excluded (they dropped timed entry for 2026).
- `roads.json` (repo root) — curated road data keyed by slug. A row publishes ONLY when
  `datesReviewed: true`. Fields: name, `parentIds` (array, may be empty), `statusUrl`
  (not assumed nps.gov), designation, blurb, `seasonal` (bool), `accessNote` (optional
  free text), `typicalOpen`/`typicalClose`, `history` (dated reopenings + source),
  `datesReviewed`, `isScenicDrive`, `reservationRef`.
- `worker.js` — Cloudflare Worker `parkstatus-api`. Hourly `scheduled()` rebuilds a KV
  blob; `GET /` serves the combined status blob (**public contract — do not break**).
  `classify()` / `derive()` bucket alerts → status + reason + counts. The blob stores
  ONE synthesized `reason` sentence per park + `counts` — **NOT raw alert text, and no
  road data.** Curated arrays `NY_PARKS` (~184), `TX_PARKS` (~556). Routes: `/rebuild`
  `/indexnow` `/geocode` `/stats` `/subscribe` `/unsubscribe` `/push/subscribe`
  `/push/unsubscribe` `/push/native/subscribe` `/push/native/test`. Subscription scope
  shape: `{kind:"all"|"parks"|"geo", parks?, lat?, lon?, radiusMi?, label?, from?, to?,
  disasters?}` — `sanitizeScope()` (web) drops `disasters`; `sanitizeNativeScope()`
  keeps it (defaults true). Push subs keyed `sub:push:<sha1(endpoint)>`; native
  `push:native:<installId>`. Deploys via Cloudflare Workers Builds (Git integration) on
  push — no manual deploy.
- `wrangler.toml`, `capacitor.config.json`, `fastlane/`, `ios/`.
- `LAUNCH-STATUS.md` — hand-maintained infra/deploy/iOS-signing reference. `MOBILE.md`,
  `README.md`.

## Coordination files (this workflow)

All at the repo root (the repo root is NOT deployed — only `public_html/**` triggers the
FTP sync — so planning docs here are safe):

- `PROJECT-CONTEXT.md` — this file. Facts + house rules. Update when the codebase moves.
- `PROGRESS.md` — dated work log + summary table. The builder appends here every turn.
- `BUILDER-PROMPTS.md` — the queue: a status table + one full prompt block per item. The
  prompt-engineer appends here; the builder marks items done.

## Build / deploy pipeline

- Push to `main` touching `public_html/**` → `.github/workflows/deploy.yml` → FTP sync to
  Hostinger. The sync **deletes** files removed from `public_html/` (`.htaccess` and
  `.well-known/` excluded). Removing/unpublishing a page 404s a URL that may be indexed.
- `.github/workflows/refresh-park-data.yml` — cron 08:00 UTC daily: runs `build-parks.js`,
  commits `public_html/park`, `parks-enriched.json`, `parks.json`, `sitemap.xml`, then
  `gh workflow run deploy.yml` explicitly (fixed in `57ca1169` — a `GITHUB_TOKEN` push
  does not trigger `on: push`). **Known gap:** it does NOT `git add public_html/road` or
  `public_html/llms.txt`, so those regenerate every run but never commit/deploy.
- Worker: deploys on push via Cloudflare Git integration.
- iOS: `git tag ios-vX.Y.Z && git push origin ios-vX.Y.Z` → `ios-testflight.yml`
  (macos-15 / Xcode 26, fastlane match, TestFlight). Details in LAUNCH-STATUS.md.

## House rules for any change

- Match the surrounding code: terse vanilla JS, no framework, no build step for the
  static site. `index.html` keeps its inline `<style>` and inline scripts.
- **`build-parks.js` has zero npm dependencies — keep it that way.** Node stdlib only.
- Front-end libraries only from cdnjs, pinned to an exact version.
- Visual language shared with half-mast.com. Tokens: `--ink #0e1726`, `--navy #0b1b35`,
  `--red #ee263b`, `--paper #f4f6f9`, `--line #dfe4ec`, `--muted #667185`.
  `--font-display:"Arial Black",Impact,Arial,sans-serif` weight 950;
  `--font-sans:Arial,Helvetica,sans-serif`. **NO web fonts anywhere.**
- `build-parks.js` output must stay backward-compatible with consumers of
  `parks-enriched.json` (the homepage click-card) — or update the consumer in the same
  change. `parks.json` / `parks-enriched.json` shape changes are high-risk; call them out.
- Nav lives in ONE place per surface: `siteNav()` in build-parks.js, and the header in
  `index.html`. Keep them in sync.
- Plan-first by default: for anything beyond a one-line change, produce a plan and wait
  for the user's approval before implementing.

## Never touch without the user explicitly asking

- Cloudflare Worker secrets and GitHub Actions secrets.
- `.github/workflows/deploy.yml` and `.github/workflows/ios-testflight.yml`.
  (`refresh-park-data.yml` may be *proposed* for the `git add` fix but not silently
  edited.)
- The KV namespace IDs in `wrangler.toml`.
- The Worker's public status contract at `GET /` (shape of the combined blob).

## Commonly gotten wrong — do not re-explain, just avoid

- The Worker status blob has **no raw alert text and no road data** — only one generic
  `reason` sentence per park. Anything needing specific alert/road info must call the NPS
  API directly (see `npsAlerts()` / `npsRich()`).
- `refresh-park-data.yml` `git add` gap (road pages + llms.txt) — see pipeline above.
- `roads.json` / `reservations.json` dates drift year to year — a wrong "typically opens"
  date on a high-traffic page is worse than no page. Verify before flipping
  `datesReviewed: true`; the user reviews date rows before approval.
- Repo lives under iCloud-synced `~/Desktop/Joshua/Project/` — bulk git is slow.
- `npm ci` on this Mac needs `--cache /tmp/<dir>` (default `~/.npm` has a perms issue).
- CocoaPods: plain `pod install` in `ios/App/` (Homebrew Ruby breaks `bundle`).

## Tracks

Typical request types: **bug fix · static-site feature (index.html) · page-generator
change (build-parks.js) · Worker change · content/guide · SEO · iOS/Capacitor · infra/CI.**

## Current SEO priority stack (2026-09-07, Semrush-informed, US)

0. **Indexation** — site not fully indexed; thin/near-duplicate templated pages the
   likely cause. Enrichment + road pages also serve this.
1. **Seasonal road-status pages** `/road/<slug>/` — highest ROI. First cut partially
   shipped (`2897960b`): 4 year-round roads live, 6 seasonal roads staged pending a
   historical-dates pass. Demand: cades cove 60.5k, going-to-the-sun road 22.2k,
   beartooth highway 22.2k, trail ridge road 14.8k, "…road status/closures/is it open/
   when does it open" at KD 20–43, weak competition.
2. **Park-page enrichment** — fees + reservations + FAQ shipped (`0b9a65e7`/`0abc6c50`).
   Deferred: holiday-hours exceptions (stale NPS dates), winter heuristic (needs curated
   data), internal links to `/road/` and a future `/state/` index.
3. **`/shutdown/` live hub** — build and index before the next funding-deadline spike.
4. **Timed-entry index + per-park sections** — don't over-invest (KD high, commercial
   SERP); play the "[park] timed entry 2026" fresh long tail.
5. **Seasonal guides** — fee-free days, holiday hours, most-visited, open-in-winter.
6. **State-park closure template** — a timing play; keep ready, deploy per fire/storm.

App direction: alerts-first onboarding; "watch a seasonal road for reopening" (validated
demand, pairs with a future Worker `/roads` endpoint); offline last-known status;
home-screen widget / Live Activity; ASO keywords: road closures, road conditions, trail
conditions, timed entry, entrance fee.
